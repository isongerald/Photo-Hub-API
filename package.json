{
  "dependencies": {
    "dotenv": "^10.0.0"
  },
  "scripts": {
    "build": "vite build",
    "postinstall": "npm install -g vite"
  },
  "devDependencies": {
    "vite": "^7.2.6"
  },
  "engines": {
    "node": ">=16"
  }
}
